# Leet DSE 資料

請將反思資料放在此目錄，命名為 `leet.csv`。此資料未隨專案提供，CSV 已由 `.gitignore` 排除。

必要欄位：`Condition`、`UserName`、`ActivityNumber`、`data.pandas`、`list.set`、`loop.loops`、`statement.conditions`。`Condition` 使用 `HDSE` 與 `LDSE`。
